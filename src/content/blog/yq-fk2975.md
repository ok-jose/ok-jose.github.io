---
title: '浏览器的事件循环机制'
description: '事件循环可以简单描述为：函数入栈，当Stack中执行到异步任务的时候，就将他丢给WebAPIs,接着执行同步任务,直到Stack为空;在此期间WebAPIs完成这个事件，把回调函数放入Callback Queue中等待;当执行栈为空时，Ev'
pubDate: 2021-04-03
updatedDate: 2021-04-03
tags: ['yuque', 'jose-wg1zg7']
source: 'https://www.yuque.com/jose/wg1zg7/fk2975'
---

事件循环可以简单描述为：

1. 函数入栈，当Stack中执行到异步任务的时候，就将他丢给WebAPIs,接着执行同步任务,直到Stack为空;
2. 在此期间WebAPIs完成这个事件，把回调函数放入Callback Queue中等待;
3. 当执行栈为空时，Event Loop把Callback Queue中的一个任务放入Stack中;
4. 回到第1步

```javascript
function demo(){
  const start = new Date();
  setTimeout(function cb(){
      console.log("时间间隔：",new Date()-start+'ms');
  },500);
while(new Date()-start<1000){
    console.log(new Date()-start)
};
}
```

![](/uploads/yuque/fk2975/1617429698793-dfbdcdcb-951d-45.png)

1. demo 函数入栈,start变量开始初始化
2. setTimeot入栈，出栈，丢给Web APIS，开始定时500ms
3. while循环入栈,开始阻塞1000ms
4. 500ms过后,WebAPIs把cb()放入任务队列,此时while循环还在栈中,cb()等待
5. 又过了500ms,  
while循环执行完毕从栈中弹出,  
demo()弹出,  
此时栈为空,Event Loop,  
cb()进入栈,  
log()进栈,  
输出'时间间隔：1003ms',出栈,  
cb()出栈
